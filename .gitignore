# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/
.env

# Pytest
.pytest_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/
*.swp

# Build artifacts
*.mcpb

# OS
.DS_Store
Thumbs.db

# Local secrets
.env.local
secrets.json
.aider*

# Claude Code (dev-only, contains session data and skills)
.claude/

# Dev-only documentation (internal topology, operational prompts, bug reports)
DEV/
BUGS/

# Test suites (contain internal URLs, user IDs, test fixtures)
TESTS/
tests/

# Test result artifacts
*.pyc
__pycache__/

# Local API-key / credential drop files. NEVER commit — this is a PUBLIC repo.
# A key file was found untracked in this working tree on 2026-07-30; these patterns
# make an accidental `git add -A` a no-op rather than a credential disclosure.
*.api
*.key
*credentials*
*secret*
